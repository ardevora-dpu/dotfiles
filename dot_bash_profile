# Quinlan Git Bash profile (Windows native)
# Managed by chezmoi - edit in the Ardevora dotfiles repository.

# Tell Claude Code where to find bash for shell operations on Git Bash.
_quinlan_uname="$(uname -s 2>/dev/null || true)"
case "$_quinlan_uname" in
    MINGW*|MSYS*|CYGWIN*)
        export CLAUDE_CODE_GIT_BASH_PATH='C:\Program Files\Git\bin\bash.exe'
        ;;
    *)
        unset CLAUDE_CODE_GIT_BASH_PATH
        ;;
esac
unset _quinlan_uname

# Workspace root for project navigation (p, dev functions)
# Derived from ~/.quinlan-repo (set by /update) — parent directory of the repo
if [[ -f ~/.quinlan-repo ]]; then
    WORKSPACE_ROOT="$(dirname "$(cat ~/.quinlan-repo)")"
    export WORKSPACE_ROOT
fi

# Source .bashrc for interactive shell customisations
if [ -f ~/.bashrc ]; then
    # shellcheck source=/dev/null
    source ~/.bashrc
fi

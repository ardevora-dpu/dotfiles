# Quinlan shell login bootstrap.
# Managed by chezmoi. Do not edit ~/.bash_profile directly.

# Keep shell initialisation in ~/.bashrc so Git Bash and WSL share one path.
if [ -f "$HOME/.bashrc" ]; then
    # shellcheck source=/dev/null
    source "$HOME/.bashrc"
fi

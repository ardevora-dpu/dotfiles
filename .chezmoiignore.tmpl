{{- /* Skip Windows-only run_once script on non-Windows hosts */ -}}
{{- if ne .chezmoi.os "windows" }}
run_once_compile-helpers.ps1
compile-helpers.ps1
{{- end }}

{{- /* Timon-only user-level skills: do not deploy for other users (e.g. Jeremy) */ -}}
{{- $isTimon := or (eq .chezmoi.username "chimern") (eq .chezmoi.username "AzureAD\\TimonvanRensburg") -}}
{{- if not $isTimon }}
dot_claude/skills/email-style/**
dot_claude/skills/mcp-config-locations/**
dot_claude/skills/merge/**
dot_claude/skills/techdebt/**
{{- end }}
